# Runtime dependencies for btc-range-model
# Pinned to the major versions known to work; relax if needed.

# Core scientific stack
numpy>=1.26
pandas>=2.1
scikit-learn>=1.8.0
joblib>=1.3

# Data feeds
yfinance>=0.2.40
requests>=2.31

# Web app
streamlit>=1.30
plotly>=5.18
# Live auto-refresh of the Overall Trading price column (optional — the app
# degrades gracefully to click/reload refresh if this is unavailable).
streamlit-autorefresh>=1.0.1

# Notebook generation
nbformat>=5.9
