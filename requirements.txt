# Runtime dependencies for btc-range-model
#
# Streamlit Community Cloud IGNORES runtime.txt and force-installs the newest
# Python (currently 3.14). Every pin here must therefore have a Python-3.14
# wheel — otherwise the platform tries to BUILD it from source, which hangs the
# deploy on the "in the oven" screen. That means: no upper bounds that would
# exclude the 3.14-wheel builds, and scikit-learn kept on the 1.9 line (the
# oldest sklearn that ships cp314 wheels).
#
# The trained model (models/inference_assets_ct.joblib) is serialized with
# scikit-learn 1.9.0 (predictions verified byte-identical to the previous 1.8.0
# build), so it loads with no version-mismatch warning. Do NOT re-pin sklearn to
# 1.8.0: that version has no cp314 wheel and reintroduces the oven hang.
#
# Verified on Python 3.13 (uv, --only-binary): all wheels, model loads clean,
# full model-predict pipeline runs with no segfault.

# Core scientific stack — floors only (need the newest, 3.14-wheel builds)
numpy>=1.26
pandas>=2.1
scipy>=1.11
scikit-learn>=1.9,<1.10   # matches the serialized model; 1.9 ships cp314 wheels
joblib>=1.3
pyarrow>=14

# Data feeds
yfinance>=0.2.40
requests>=2.31

# Web app  (no upper bound on streamlit — the platform manages its version)
streamlit>=1.30
plotly>=5.18
# Live auto-refresh of the Overall Trading price column (optional — the app
# degrades gracefully to click/reload refresh if this is unavailable).
streamlit-autorefresh>=1.0.1

# Notebook generation
nbformat>=5.9
